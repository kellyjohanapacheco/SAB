@model IEnumerable<SAB.Models.Bien>

@{
    ViewBag.Title = "listado bien";
}

<h2>index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
           Tipo Bien
        </th>
        <th>
           Numero Serial
        </th>
        <th>
            Valor
        </th>
        <th>
            Nombre Persona Cargo
        </th>
       
       
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TipoBien)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NumeroSerial)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Valor)
        </td>
       
        <td>
            @Html.DisplayFor(modelItem => item.NombrePersonaCargo)
        </td>
       
       
        <td>
            @Html.ActionLink("Edit", "Edit", new {  id=item.NumeroSerial }) |
            @Html.ActionLink("Details", "Details", new { id = item.NumeroSerial }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.NumeroSerial })
        </td>
    </tr>
}

</table>
